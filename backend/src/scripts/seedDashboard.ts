// import mongoose from 'mongoose';
// import dotenv from 'dotenv';

// dotenv.config();

// // Note: Dashboard data is now calculated dynamically in the dashboard controller
// // This seed script is deprecated and no longer needed

// async function seedDashboard() {
//   try {
//     await mongoose.connect(process.env.MONGODB_URI || 'mongodb://localhost:27017/HRMS');
//     console.log('Connected to MongoDB');

//     console.log('Dashboard data is now calculated dynamically in the dashboard controller.');
//     console.log('This seed script is no longer needed.');
//     console.log('Please use the main seed script: npm run seed');

//     process.exit(0);
//   } catch (error) {
//     console.error('Error:', error);
//     process.exit(1);
//   }
// }

// seedDashboard();
